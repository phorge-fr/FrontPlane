apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: traefik-local
  namespace: traefik
spec:
  interval: 30m
  chart:
    spec:
      chart: traefik
      version: "*"
      sourceRef:
        kind: HelmRepository
        name: traefik
        namespace: traefik
      interval: 12h
  values:
    fullnameOverride: traefik-local
    ingressClass:
      enabled: true
      isDefaultClass: false
      name: traefik-local
    providers:
      kubernetesIngress:
        ingressClass: traefik-local
    service:
      type: LoadBalancer
      annotations:
        lbipam.cilium.io/ips: "10.0.0.10"
    ports:
      web:
        http:
          redirections: {}